#CLA ADL example for using variables with 


region   testNameM
  select      ALL                    # to count all events
  select      Size (MUO) >  1        # events with 2 or more muons
  select      MET > 500   # MET example
  select      MetSig > 0.5   # MET significance example

